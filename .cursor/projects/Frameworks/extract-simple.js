// Simple one-line extraction - copy this entire line and paste after typing "allow pasting"
(function(){const d=document.body.innerText;const h=Array.from(document.querySelectorAll('h1,h2,h3,h4,h5,h6')).map(h=>({level:parseInt(h.tagName[1]),text:h.textContent.trim()}));const l=Array.from(document.querySelectorAll('ul,ol')).map(ul=>Array.from(ul.querySelectorAll('li')).map(li=>li.textContent.trim()));const p=Array.from(document.querySelectorAll('p')).map(p=>p.textContent.trim()).filter(t=>t.length>20);const r={headings:h,lists:l,paragraphs:p,fullText:d.substring(0,10000)};const j=JSON.stringify(r,null,2);const b=new Blob([j],{type:'application/json'});const u=URL.createObjectURL(b);const a=document.createElement('a');a.href=u;a.download='framework-simple.json';a.click();console.log('✅ Extracted! File downloaded as framework-simple.json');return r;})();
